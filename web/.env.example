# Google Maps JavaScript SDK API key.
# Required for the interactive map to load.
# Obtain a key from https://console.cloud.google.com/ (enable Maps JavaScript API).
# Without this key the map container renders in a placeholder state.
VITE_GOOGLE_MAPS_API_KEY=your_api_key_here
